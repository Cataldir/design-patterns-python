# Lab L1 — Refactor a Switch-Heavy Code Smell Using Strategy

Start with `starter.py` which contains a discount calculator using if-else chains.
Refactor it into the Strategy pattern. Compare your result with `solution.py`.

## Run Tests

```bash
pytest test_lab.py -v
```
